    DMA-IN  ioport dest len --

    Transfers len bytes from an I/O port to memory address dest using
    the DMA controller in continuous mode. The destination memory
    address increments after each byte, while the I/O port address
    remains fixed. This operation blocks the CPU until completion.

