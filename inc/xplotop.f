\
\ xplotop.f
\
\ pixel operator vector used by XPLOT; defaults to XOR ("xor")
\
.( XPLOTOP )

NEEDS DEFER
NEEDS IS
NEEDS L0-XOR

DEFER XPLOTOP
' L0-XOR IS XPLOTOP      \ usually XOR to "xor" the pixel

